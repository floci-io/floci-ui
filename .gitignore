# Dependencies
node_modules/

# npm/yarn lockfiles are local noise for this pnpm workspace
package-lock.json
yarn.lock

# The Release Cut toolchain is pinned; its lockfile must be committed
# (npm ci fails without it) despite the global package-lock.json ignore.
!.github/release-tooling/package-lock.json

# Build output
dist/
dist-ssr/
build/

# Tool caches
.vite/
.pnpm-store/
.turbo/
.parcel-cache/
.cache/
.eslintcache

# Test and coverage output
coverage/
playwright-report/
test-results/
.nyc_output/

# Environment files
.env
.env.*
!.env.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# TypeScript
*.tsbuildinfo

# Local editor and IDE state
.idea/
.vscode/
*.swp
*.swo

# Local assistant/tool state
.claude/

# Agent instruction symlinks — AGENTS.md is the canonical tracked file.
# Contributors symlink these locally (see CONTRIBUTING.md); don't commit them.
CLAUDE.md
GEMINI.md

# OS files
.DS_Store
Thumbs.db

# Temporary files
tmp/
temp/
*.tmp
*.temp

data
local
